{
  "private": true,
  "name": "focal",
  "scripts": {
    "build": "yarn workspace @grammarly/focal build",
    "package": "yarn workspace @grammarly/focal pack",
    "release": "yarn workspace @grammarly/focal release",
    "test": "yarn workspace @grammarly/focal test",
    "postinstall": "yarn build",
    "test:ci": "scripts/run_each test"
  },
  "devDependencies": {
    "@grammarly/tslint-config": "0.5.1",
    "tslint": "5.2.0",
    "typescript": "2.8.4"
  },
  "workspaces": [
    "packages/focal",
    "packages/manual",
    "packages/examples/*"
  ],
  "resolutions": {
    "@types/react": "16.0.33"
  }
}
